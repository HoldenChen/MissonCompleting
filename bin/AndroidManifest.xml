<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.missioncompleting"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
         >
        <activity
            android:name="com.holden.missioncompleting.MissionListActivity"
            android:label="@string/app_name" 
            
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.holden.missioncompleting.AddDetailActivity"
            android:label="@string/AddDetails"
            
			>
            <intent-filter>
            </intent-filter>
        </activity>
        
         <activity
            android:name="com.holden.missioncompleting.EditMissionDetailsActivity"
            android:label="@string/EditDetails"
            
			>
            <intent-filter>
            </intent-filter>
        </activity>
        
        <activity
            android:name="com.holden.missioncompleting.ShowAlarmActivity"
            android:label="@string/showAlarm"
            
			>
            <intent-filter>
            </intent-filter>
        </activity>
        
        <receiver android:name="com.holden.missioncompleting.util.AlarmReceiver" android:process=":remote"/>   
				
		 
    </application>

</manifest>
